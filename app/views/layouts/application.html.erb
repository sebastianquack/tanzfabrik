<!DOCTYPE html>
<html lang="de"><!-- TODO: lang -->
<head>  
  <title>Tanzfabrik</title>
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>  
  <%= stylesheet_link_tag "application_edit", media: "all", "data-turbolinks-track" => true if admin_user_signed_in? %> 
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application_edit", "data-turbolinks-track" => true if admin_user_signed_in? %>
  <%= google_webfonts_link_tag :roboto => [400, 700] %>
  <%= csrf_meta_tags %>
    
	<!--[if lt IE 9]>
	  <%= javascript_include_tag 'html5shiv-printshiv' %>
    <style>
      #header:after, #header:before { background:none; }
      #secondary-menu  { position:relative; display:block; clear:both; width:100%; text-align:left;}
      #language-select { position:relative; display:block; clear:both; width:100%; }
      ul.navigation ul ul {background:white; border: 1px solid black; padding:0.5em;}
    </style>
	<![endif]-->  
</head>
<body>

  <!-- hier das Hintergrundbild -->
  <% if Festival.where(:feature_on_welcome_screen => true).length > 0 %>
    <%= image_tag Festival.where(:feature_on_welcome_screen => true).first.images.first.attachment.url(:medium) %>
  <% elsif Event.where(:feature_on_welcome_screen => true).length > 0 %>
    <%= image_tag Image.where(:feature_on_welcome_screen => true).first.images.first.attachment.url(:medium) %>
  <% elsif Image.where(:show_on_welcome_screen => true).length > 0 %>
    <!-- hier müsste man noch täglich wechseln mit pseudorandom -->
    <%= image_tag Image.where(:show_on_welcome_screen => true).first.attachment.url(:medium) %>
  <% end %>

<div id="overall-container">
	<div class="container">
		<%= render "/navigation/default" %>
    <div id="content-container">
		  <section id="content">
			 <%= yield %>
		  </section>
    </div>
	</div>
</div>

</body>
</html>
