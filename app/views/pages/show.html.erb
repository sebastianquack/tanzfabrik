<% if @page %>

    <h1 id="page_title" data-mercury="simple"><%= @page.title %></h1>

    <div id="page_content" data-mercury="full">
        <%= @page.content.html_safe %>
    </div>

    <%= render "admin_link" %>

<% end %>

<%= render "/navigation/default" %>
