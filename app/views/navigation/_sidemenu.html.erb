<% dividers = @page.content_modules.submenu_dividers %>

<% if dividers.length > 0 %>

  <ul class="side_menu">
  
    <% for cm in dividers %>
  
      <li>
        <a href="#<%= cm.parameter %>">
          <%= cm.headline %>
        </a>
      </li>
  
    <% end %>
  
  </ul>

<% end %>